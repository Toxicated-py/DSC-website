APP_ENV=development
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_PUBLISHABLE_KEY=paste-local-publishable-key-here

# Required in production for backend-only admin reads and certificate verification.
# Use this only on the Python backend. Never expose it to Vite/frontend code.
# SUPABASE_SERVICE_ROLE_KEY=paste-service-role-key-here
